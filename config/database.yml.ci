default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  username: postgres
  password: <%= ENV.fetch("DB_PASSWORD") { "password" } %>
  host: localhost

test:
  <<: *default
  database: myapp_test
