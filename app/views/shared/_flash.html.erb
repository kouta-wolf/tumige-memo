<% flash.each do |message_type, message| %>
  <div class="mb-4 p-4 rounded-xl text-sm font-bold shadow-sm border
              <%= message_type == 'notice' ? 'bg-green-50 text-green-700 border-green-200' : 'bg-red-50 text-red-700 border-red-200' %>">
    <%= message %>
  </div>
<% end %>
